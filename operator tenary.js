const age = 25;
const isAdult = age >= 18 ? "Adult" : "Not Adult";

console.log(isAdult);

const score = 75;
const result = score >= 80 ? "Excellent" : score >= 60 ? "Pass" : "Fail";

console.log(result);
